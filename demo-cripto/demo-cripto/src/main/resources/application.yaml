spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/criptopulse
    username: postgres
    password: admin123
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  flyway:
    enabled: true

app:
  default-user-id: 11111111-1111-1111-1111-111111111111